{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-quick",
      "type": "shell",
      "command": "build-quick",
      "args": [
      ],
      "group": "build",
      "problemMatcher": [
        "$rustc"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },
    {
      "label": "cargo-build-only",
      "type": "shell",
      "command": "cargo",
      "args": [
        "build"
      ],
      "group": "build",
      "problemMatcher": [
        "$rustc"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}