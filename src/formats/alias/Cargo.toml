[package]
name = "ctb-formats-alias"
edition = "2024"

[lib]
path="alias.rs"
crate-type = ["lib"]

[dependencies]
ctb-utilities = { path = "../../utilities" }
include_dir = { version = "0.7.4", features = ["glob"] }

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
