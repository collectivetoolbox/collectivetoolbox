[package]
name = "ctb-formats-hexdump"
edition = "2024"

[lib]
path="hexdump.rs"
crate-type = ["lib"]

[dependencies]
ctb-utilities = { path = "../../utilities" }
ctb-formats-encoding = { path = "../encoding" }
ctb-formats-utilities = { path = "../utilities" }

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
