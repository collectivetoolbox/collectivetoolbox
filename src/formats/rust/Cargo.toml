[package]
name = "ctb-formats-rust"
edition = "2024"

[lib]
path="rust.rs"
crate-type = ["lib"]

[dependencies]
ctb-utilities = { path = "../../utilities" }
ctb-formats-utilities = { path = "../utilities" }
include_dir = { version = "0.7.4", features = ["glob"] }

check_keyword = "0.4.1"

[lints]
workspace = true
