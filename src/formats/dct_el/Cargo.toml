[package]
name = "ctb-formats-dct-el"
edition = "2024"

[lib]
path="dct_el.rs"
crate-type = ["lib"]

[dependencies]
ctb-utilities = { path = "../../utilities" }
ctb-formats-utilities = { path = "../utilities" }
include_dir = { version = "0.7.4", features = ["glob"] }

[lints]
workspace = true
