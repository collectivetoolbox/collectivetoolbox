[package]
name = "ctb-formats-utilities"
edition = "2024"

[lib]
path="utilities.rs"
crate-type = ["lib"]

[dependencies]
ctb-utilities = { path = "../../utilities" }
include_dir = { version = "0.7.4", features = ["glob"] }

[lints]
workspace = true
