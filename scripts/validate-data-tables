#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd -- "${SCRIPT_DIR}/.." && pwd)"

cargo run \
    --quiet \
    --manifest-path "${REPO_ROOT}/src/formats/data_validator/Cargo.toml" \
    "$@" \
    --bin validate-data-tables
